#include "main.h"
#include <ctype.h>

/**
 * _isdigit - checks if a passed character is a digit from 0 through 9  or not
 * Return: 1
 */

int _isdigit(int c)
{
	char c;
	
       	c = '0';

	if (isdigit(c))
		putchar(c);
	return (1);
	
	else (c = 'a')
	return (0);
}


	

